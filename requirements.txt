# Core
numpy==1.24.1
pillow==10.2.0
pyyaml
tqdm==4.66.1
setuptools==68.0.0

# PyTorch (CUDA 12.8) — install via index URL (see commands below)
# pip install torch==2.7.0+cu128 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# torch==2.7.0+cu128
# torchvision
# torchaudio

# Vision / 3D
opencv-python==4.8.1.78
open3d==0.18.0
plyfile==1.0.3

# Scientific
scipy==1.12.0
scikit-learn
matplotlib
h5py==3.10.0

# Build tools
pybind11>=2.11
ninja

# Gaussian Splatting submodules — install separately (see commands below)
# -e gaussian-splatting/submodules/diff-gaussian-rasterization
# -e gaussian-splatting/submodules/fused-ssim
# -e gaussian-splatting/submodules/simple-knn